# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
ehthumbs.db
desktop.ini

# Editors
.vscode/
.idea/
*.swp
*.swo
*~

# Chrome extension packaging
# *.crx is the signed package, *.pem is the private signing key — never commit either.
*.zip
*.crx
*.pem

# Native messaging host (Phase 3.5, installed locally, not part of this repo)
native-host/

# Local logs / scratch
*.log
.env
